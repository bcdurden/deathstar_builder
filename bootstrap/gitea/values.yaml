ingress:
  enabled: true
  hosts:
  - host: git.sienarfleet.systems
    paths: 
    - path: "/"
      pathType: "Prefix"
  tls:
  - secretName: gitea-prod-certificate
    hosts:
    - git.sienarfleet.systems